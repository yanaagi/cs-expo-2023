import ProjectList from "../list";
import { NextPage } from "next";
const PageProjectList:NextPage = () => {
  return (
    <section id="project-list">
      <ProjectList/>
    </section>
  )
}

export default PageProjectList;